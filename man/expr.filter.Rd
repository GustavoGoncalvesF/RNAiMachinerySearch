% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression-filtering.R
\name{expr.filter}
\alias{expr.filter}
\title{Filter RNAi hits based on expression thresholds}
\usage{
expr.filter(
  raw_rnai_hits,
  expression_df,
  groups_df,
  cpm_cut_group = 1,
  cpm_cut_global = 10
)
}
\arguments{
\item{raw_rnai_hits}{A data frame containing RNAi hits, typically generated by \code{\link{search.rnai}}.}

\item{expression_df}{A data frame of raw counts with contigs as rows and sample names as columns.}

\item{groups_df}{A data frame describing sample groupings, with columns \code{SAMPLE} and \code{REP}.}

\item{cpm_cut_group}{Numeric. Threshold CPM value for the group minimum expression filter. Default is 1.}

\item{cpm_cut_global}{Numeric. Threshold CPM value for the global minimum expression filter. Default is 10.}
}
\value{
A filtered RNAi hits data frame containing only the contigs that pass both filters.
}
\description{
This function filters the raw RNAi hits data frame to retain only contigs
that are relevant in term of expression. The filtering is performed in two steps:
\enumerate{
\item \strong{Group minimum expression criterion}: keeps a gene if it has CPM greater than
or equal to \code{cpm_cut_group} in all samples of at least one group.
\item \strong{Global minimum expression criterion}: keeps a gene if it has CPM greater than
or equal to \code{cpm_cut_global} in at least the minimal number of replicates across all samples.
}
}
\details{
This function uses the \code{edgeR::cpm()} function to calculate Counts Per Million (CPM) from raw counts.
It prints a report summarizing the number of genes removed at each filtering step.
}
\examples{
\dontrun{
# Filtering RNAi hits using default thresholds:
raw_rnai_hits <- search.rnai(annotation_df = annotation, column = "Top_BLASTX_hit")
expression_df <- read.table("raw_counts.txt")
groups_df <- read.table("samples.txt")


filtered_rnai_hits <- expr.filter(raw_rnai_hits, expression_df, groups_df)
}

}
